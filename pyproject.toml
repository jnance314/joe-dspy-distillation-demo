[project]
name = "dspy-demo"
version = "0.2.0"
description = "DSPy prompt optimizer — automatic prompt optimization with MIPROv2"
requires-python = ">=3.10"
dependencies = [
    "dspy",
    "python-dotenv",
    "fastapi",
    "uvicorn[standard]",
]
